{% macro returnString code operation %}return {{ code }}, {% for code,response in operation.responses %}response{{ code }}, {% endfor %}defaultResponse{% endmacro %}

{% macro encodeError operation %}
    if err != nil {
        defaultResponse.Code = 500
        defaultResponse.Message = err.Error()
        {% call returnString "500" operation %}
    }
{% endmacro %}